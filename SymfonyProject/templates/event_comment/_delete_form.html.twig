<form method="post" action="{{ path('event_comment_delete', {'id': event_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_comment.id) }}">
    <button class="btn">Delete</button>
</form>
