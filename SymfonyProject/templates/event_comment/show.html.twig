{% extends 'base.html.twig' %}

{% block title %}EventComment{% endblock %}

{% block body %}
    <h1>EventComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_comment.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ event_comment.createdAt ? event_comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ event_comment.rating }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ event_comment.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_comment_index') }}">back to list</a>

    <a href="{{ path('event_comment_edit', {'id': event_comment.id}) }}">edit</a>

    {{ include('event_comment/_delete_form.html.twig') }}
{% endblock %}
