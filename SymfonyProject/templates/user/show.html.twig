{% extends 'backoffice.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}


    <table class="table" >
        <tbody>

            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ user.photo }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ user.city }}</td>
            </tr>
            <tr>
                <th>Gouvernorat</th>
                <td>{{ user.gouvernorat }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user.phone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ user.mail }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
            <tr>
                <th>MontantDonne</th>
                <td>{{ user.montantDonne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'userid': user.userid}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
