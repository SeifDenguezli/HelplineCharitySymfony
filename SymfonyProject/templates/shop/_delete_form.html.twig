<form method="post" action="{{ path('shop_delete', {'shopId': shop.shopId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop.shopId) }}">
    <button class="btn">Delete</button>
</form>
