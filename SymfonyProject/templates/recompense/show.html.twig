{% extends 'backoffice.html.twig' %}

{% block title %}Recompense{% endblock %}

{% block body %}
    <h1>Recompense</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Recompenseid</th>
            <td>{{ recompense.recompenseid }}</td>
        </tr>
        <tr>
            <th>Nbrepoints</th>
            <td>{{ recompense.nbrepoints }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('recompense_index') }}">back to list</a>

    <a href="{{ path('recompense_edit', {'recompenseid': recompense.recompenseid}) }}">edit</a>

    {{ include('recompense/_delete_form.html.twig') }}
{% endblock %}