<form method="post" action="{{ path('admin_service_delete', {'serviceid': service.serviceId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.serviceId) }}">
    <button class="btn ">Delete</button>
</form>
