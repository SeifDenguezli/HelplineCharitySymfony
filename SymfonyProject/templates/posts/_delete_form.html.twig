<form method="post" action="{{ path('posts_delete', {'postid': post.postid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.postid) }}">
    <button class="btn">Delete</button>
</form>
