<form method="post" action="{{ path('evenement_delete', {'eventid': evenement.eventid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.eventid) }}">
    <button class="btn">Delete</button>
</form>
