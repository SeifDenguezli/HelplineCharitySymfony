<form method="post" action="{{ path('admin_evenement_delete', {'eventId': evenement.eventId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.eventId) }}">
    <button class="btn">Delete</button>
</form>
